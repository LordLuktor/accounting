# Frontend Configuration
VITE_API_URL=https://api.accuflow.local

# Docker Configuration
FRONTEND_DOMAIN=accuflow.local
BACKEND_DOMAIN=api.accuflow.local
BACKEND_URL=https://api.accuflow.local
FRONTEND_URL=https://accuflow.local

# SSL Configuration
ACME_EMAIL=admin@example.com

# JWT Secret (change this!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Database (handled by docker-compose)
DB_HOST=postgres
DB_PORT=5432
DB_USER=accuflow
DB_PASSWORD=password
DB_NAME=accuflow